<Page
    x:Class="temalab.TransactionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:temalab"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <controls:DataGrid x:Name="TransactionsGrid" IsReadOnly="True">
        <controls:DataGrid.Columns>
            <controls:DataGridTextColumn Header="Name"/>
            <controls:DataGridTextColumn Header="Amount"/>
            <controls:DataGridTextColumn Header="Category"/>
            <controls:DataGridTextColumn Header="Description"/>
            <controls:DataGridTextColumn Header="Date"/>
        </controls:DataGrid.Columns>
    </controls:DataGrid>
</Page>
