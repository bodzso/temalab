<Page
    x:Class="temalab.TransactionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:temalab"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <controls:DataGrid x:Name="TransactionsGrid" IsReadOnly="True" CanUserReorderColumns="False" CanUserSortColumns="True" Sorting="TransactionsGrid_Sorting" AutoGenerateColumns="False">
        <controls:DataGrid.Columns>
            <controls:DataGridTextColumn Header="Name" Binding="{Binding name}"/>
            <controls:DataGridTextColumn Header="Amount" Binding="{Binding amount}"/>
            <controls:DataGridTextColumn Header="Category" Binding="{Binding categoryName}"/>
            <controls:DataGridTextColumn Header="Description" Binding="{Binding description}"/>
            <controls:DataGridTextColumn Header="Date" Binding="{Binding date}"/>
        </controls:DataGrid.Columns>
    </controls:DataGrid>
</Page>
