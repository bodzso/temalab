<Page
    x:Class="temalab.LoginAsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:temalab"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="15" Width="300">
        <TextBlock Text="Login as" FontSize="30" HorizontalAlignment="Left"/>
        <ListBox x:Name="knownUsersList" ItemsSource="{x:Bind Path=knownUsers}"/>
        <StackPanel Spacing="10" Visibility="Collapsed">
            <PasswordBox x:Name="passwordBox" PlaceholderText="Enter password" Visibility="Visible"/>
            <Button x:Name="loginButton" Content="Login" Visibility="Visible"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Spacing="20">
            <Button Content="Login" Width="140" Click="ToLoginPage"/>
            <Button Content="Register" Width="140" Click="ToRegisterPage"/>
        </StackPanel>
        <!-- ami innentől jön lehet csak az után kellene megjelenjen, hogy kiválasztottuk melyik felh
        jelentkezik be-->
    </StackPanel>
</Page>
