<Page
    x:Class="temalab.LoginScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:temalab"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="15">
        <TextBlock Text="Login" FontSize="30" HorizontalAlignment="Left"/>
        <TextBox x:Name="usernameBox" PlaceholderText="Username" Width="300"/>
        <PasswordBox x:Name="passwordBox" PlaceholderText="Password" Width="300"/>
        <CheckBox x:Name="rememberMeCheck" Content="Remember me"/>
        <StackPanel Orientation="Horizontal" Spacing="20">
            <Button x:Name="loginButton" Content="Login" Width="140" Background="Red"/>
            <Button x:Name="registerButton" Content="Register" Width="140" Background="Red"/>
        </StackPanel>
    </StackPanel>
</Page>
