<Page
    x:Class="temalab.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:temalab"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
        <TextBlock Text="Login" FontSize="30" HorizontalAlignment="Left"/>
        <TextBox Header="Username" PlaceholderText="Enter username" x:Name="username" Width="300"/>
        <PasswordBox Header="Password" PlaceholderText="Enter password" x:Name="password" Width="300"/>
        <CheckBox x:Name="rememberMeCheck" Content="Remember me"/>
        <StackPanel Orientation="Horizontal" Spacing="20">
            <Button Content="Login" x:Name="loginButton" Width="140" Click="Login"/>
            <Button Content="Register" x:Name="registerButton" Width="140" Click="ToRegisterPage"/>
        </StackPanel>
    </StackPanel>
</Page>
